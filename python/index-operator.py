# index operator [] = gives acces to a sequences element 

name = "bro Code"

#if(name[0].islower()):
    #name = name.capitalize()

first_name = name[0:3].upper()
last_name = name[4:].lower()
last_chararacter = name[-1]

print(first_name)
print(last_name)
print(last_chararacter)
